{
  "rabbitmq": {
    "protocol": "amqp",
    "hostname": "localhost",
    "port": 5672,
    "username": "guest",
    "password": "guest",
    "queue_config": {
      "hello-world": {
        "vhost": "/personal-rabbit-project",
        "exchange": "hello-world",
        "exchangeType": "topic",
        "queue": "hello-world",
        "routingKey": "#"
      },
      "retry-events": {
        "vhost": "/personal-rabbit-project",
        "exchange": "retry_exchange",
        "exchangeType": "topic"
      }
    }
  }
}